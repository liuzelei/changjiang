<%= form_for(@order_accessory) do |f| %>
  <% if @order_accessory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_accessory.errors.count, "error") %> prohibited this order_accessory from being saved:</h2>

      <ul>
      <% @order_accessory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :accessory_id %><br>
    <%= f.number_field :accessory_id %>
  </div>
  <div class="field">
    <%= f.label :count %><br>
    <%= f.number_field :count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
